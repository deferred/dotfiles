---
name: tmux
description: Use whenever working with tmux-related files, including tmux.conf, hooks, scripts, tests, and docs.
---

# tmux

- Use `{ ... }` for multiline tmux command lists. Pass multiword shell code to `run-shell` as one quoted
  argument; braces do not quote shell code.
