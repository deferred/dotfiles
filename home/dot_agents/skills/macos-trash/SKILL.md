---
name: macos-trash
description: >
  Use when deleting files or directories on macOS. Move targets to the macOS Trash with the built-in
  `trash` command instead of using `rm`, including for cleanup, generated files, and folders.
---

# macOS Trash

On macOS, use `trash "<path>"` instead of `rm` for files and directories. Pass more paths as
additional quoted arguments.
